apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: utfsm
  labels:
    app.kubernetes.io/name: chat-service
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  selector:
    app.kubernetes.io/name: chat-service
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
